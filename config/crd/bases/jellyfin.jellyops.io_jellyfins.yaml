---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: jellyfins.jellyfin.jellyops.io
spec:
  group: jellyfin.jellyops.io
  names:
    kind: Jellyfin
    listKind: JellyfinList
    plural: jellyfins
    shortNames:
    - jf
    singular: jellyfin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Jellyfin is the Schema for the jellyfins API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Jellyfin
            properties:
              api:
                description: |-
                  API enables day-2 reconciliation of in-app state (libraries) via Jellyfin's
                  HTTP API on :8096 (see spec §7.6).
                properties:
                  credentialsSecret:
                    description: |-
                      CredentialsSecret holds admin credentials. Keys: apiKey, or
                      username + password.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  generatedSecretName:
                    description: |-
                      GeneratedSecretName is the operator-owned Secret where a bootstrapped API
                      key is stored so bootstrap is idempotent and survives restarts.
                    type: string
                  manageLibraries:
                    description: |-
                      ManageLibraries enables reconciliation of Jellyfin virtual folders against
                      storage.media[].library.
                    type: boolean
                  mode:
                    default: bootstrap
                    description: |-
                      Mode selects how the operator obtains an admin API key.
                        provided  - read an existing key (or username/password) from
                                    credentialsSecret.
                        bootstrap - drive the first-run wizard, mint a key, and persist it into
                                    generatedSecretName.
                    enum:
                    - provided
                    - bootstrap
                    type: string
                  prune:
                    description: |-
                      Prune removes managed libraries that are no longer declared. Only libraries
                      previously created by the operator are ever removed.
                    type: boolean
                  refreshLibraryOnChange:
                    description: RefreshLibraryOnChange triggers a Jellyfin library
                      scan after any change.
                    type: boolean
                type: object
              gateway:
                description: Gateway configures a Gateway API HTTPRoute. Mutually
                  exclusive with Ingress.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are applied to the HTTPRoute.
                    type: object
                  gatewayRef:
                    description: GatewayRef identifies the Gateway resource this HTTPRoute
                      attaches to.
                    properties:
                      name:
                        description: Name is the Gateway resource name.
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the Gateway's namespace. When empty the HTTPRoute's own
                          namespace is assumed.
                        type: string
                      sectionName:
                        description: SectionName is a specific listener on the Gateway
                          (e.g. "https").
                        type: string
                    required:
                    - name
                    type: object
                  hostname:
                    description: Hostname is the DNS name the HTTPRoute matches on.
                    minLength: 1
                    type: string
                  sso:
                    description: SSO configures OIDC/Keycloak auto-login behaviour
                      at the gateway.
                    properties:
                      authorizePath:
                        default: /sso/authorize
                        description: AuthorizePath is the plugin authorize endpoint.
                          Defaults to "/sso/authorize".
                        type: string
                      autoLoginRedirect:
                        description: |-
                          AutoLoginRedirect, when true, makes the entry route redirect to AuthorizePath
                          instead of /web/, so unauthenticated visitors are sent straight to Keycloak.
                        type: boolean
                    type: object
                required:
                - gatewayRef
                - hostname
                type: object
              hardwareAcceleration:
                description: HardwareAcceleration optionally attaches a GPU/VAAPI
                  device for transcoding.
                properties:
                  devicePath:
                    description: |-
                      DevicePath is the render device to mount for vaapi/qsv, e.g.
                      /dev/dri/renderD128. Ignored for nvidia (handled via device plugin).
                    type: string
                  renderGroupGID:
                    description: |-
                      RenderGroupGID is added as a supplemental group so the container can access
                      the render device.
                    format: int64
                    type: integer
                  runtimeClassName:
                    description: |-
                      RuntimeClassName selects a runtime class for nvidia acceleration, e.g.
                      "nvidia".
                    type: string
                  type:
                    description: Type selects the acceleration backend.
                    enum:
                    - vaapi
                    - qsv
                    - nvidia
                    type: string
                required:
                - type
                type: object
              image:
                description: |-
                  Image is the Jellyfin server container image. Defaults to a stock/official
                  Jellyfin image when empty. Override only when a plugin requires an
                  ABI-incompatible server build (see spec §4.4).
                type: string
              imagePullSecrets:
                description: |-
                  ImagePullSecrets are attached to the Jellyfin pod for pulling the server
                  and plugin images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              ingress:
                description: |-
                  Ingress optionally exposes the instance over HTTP(S). Mutually exclusive
                  with Gateway.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are applied to the Ingress.
                    type: object
                  className:
                    description: ClassName selects the IngressClass, e.g. "nginx".
                    type: string
                  host:
                    description: Host is the external hostname, e.g. jellyfin.example.com.
                    minLength: 1
                    type: string
                  tls:
                    description: TLS optionally enables TLS termination using the
                      referenced Secret.
                    properties:
                      secretName:
                        description: SecretName references a kubernetes.io/tls Secret
                          for the host.
                        minLength: 1
                        type: string
                    required:
                    - secretName
                    type: object
                required:
                - host
                type: object
              pluginSelector:
                description: |-
                  PluginSelector selects JellyfinPlugins bound to this instance by label, in
                  addition to plugins that reference the instance directly via jellyfinRef.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              podAnnotations:
                additionalProperties:
                  type: string
                description: PodAnnotations are merged onto the Jellyfin pod template
                  metadata.
                type: object
              replicas:
                description: |-
                  Replicas is the number of Jellyfin pods. Jellyfin is not active-active, so
                  this is typically 1.
                format: int32
                minimum: 0
                type: integer
              resources:
                description: Resources sets the Jellyfin container resource requests/limits.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              service:
                description: Service configures the ClusterIP/LoadBalancer Service
                  fronting :8096.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are applied to the Service (e.g. load-balancer
                      hints).
                    type: object
                  port:
                    description: Port is the Service port exposing Jellyfin. Defaults
                      to 8096.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  type:
                    description: Type is the Service type. Defaults to ClusterIP.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              storage:
                description: Storage configures the config/cache PVCs and media library
                  folders.
                properties:
                  cache:
                    description: Cache is an optional PVC for Jellyfin's transcode/image
                      cache.
                    properties:
                      accessModes:
                        description: |-
                          AccessModes for a provisioned PVC. Defaults to ReadWriteOnce for config and
                          ReadWriteMany for shared media.
                        items:
                          type: string
                        type: array
                      existingClaim:
                        description: |-
                          ExistingClaim references an already-provisioned PVC by name. When set, Size
                          and StorageClassName are ignored and the operator creates nothing.
                        type: string
                      mountPath:
                        description: MountPath overrides the default mount path inside
                          the pod.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the requested capacity when the operator
                          provisions the PVC.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName selects the storage class for
                          a provisioned PVC.
                        type: string
                    type: object
                  config:
                    description: |-
                      Config is the RWO PVC backing Jellyfin's /config (settings, metadata,
                      plugin state). Mounted writable so Jellyfin can mutate plugin meta.json.
                    properties:
                      accessModes:
                        description: |-
                          AccessModes for a provisioned PVC. Defaults to ReadWriteOnce for config and
                          ReadWriteMany for shared media.
                        items:
                          type: string
                        type: array
                      existingClaim:
                        description: |-
                          ExistingClaim references an already-provisioned PVC by name. When set, Size
                          and StorageClassName are ignored and the operator creates nothing.
                        type: string
                      mountPath:
                        description: MountPath overrides the default mount path inside
                          the pod.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the requested capacity when the operator
                          provisions the PVC.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName selects the storage class for
                          a provisioned PVC.
                        type: string
                    type: object
                  media:
                    description: |-
                      Media is the set of library folders mounted into the Jellyfin pod. A folder
                      with a library block is also registered as a Jellyfin library via the API.
                    items:
                      description: |-
                        MediaFolder is one library folder mounted into the Jellyfin pod. Exactly one
                        source (nfs, pvc, or existingClaim) must be set.
                      properties:
                        existingClaim:
                          description: ExistingClaim mounts an already-provisioned
                            PVC by name.
                          type: string
                        library:
                          description: |-
                            Library, when set, registers this folder as a Jellyfin library via the API
                            (requires spec.api.manageLibraries). Absent means mounted-but-not-registered.
                          properties:
                            collectionType:
                              description: CollectionType selects the library kind,
                                e.g. movies, tvshows, music.
                              enum:
                              - movies
                              - tvshows
                              - music
                              - musicvideos
                              - homevideos
                              - boxsets
                              - books
                              - mixed
                              type: string
                            name:
                              description: Name is the library display name. Defaults
                                to the MediaFolder name.
                              type: string
                            options:
                              description: |-
                                Options are opaque Jellyfin LibraryOptions passed through verbatim on
                                create/update.
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        mountPath:
                          description: |-
                            MountPath is the absolute path the folder is mounted at inside the pod,
                            e.g. /media/movies.
                          minLength: 1
                          type: string
                        name:
                          description: |-
                            Name identifies the folder, e.g. "movies". Used for the volume name and,
                            when no library.name is given, as the Jellyfin library display name.
                          minLength: 1
                          type: string
                        nfs:
                          description: NFS mounts an NFS export directly (inline)
                            or via a provisioned PV+PVC.
                          properties:
                            mountOptions:
                              description: |-
                                MountOptions applied to the provisioned PV, e.g. ["nfsvers=4.1","hard"].
                                Ignored for inline NFS (Kubernetes NFSVolumeSource has no mount options).
                              items:
                                type: string
                              type: array
                            path:
                              description: Path is the exported path on the server.
                              minLength: 1
                              type: string
                            provision:
                              description: |-
                                Provision, when true, creates a shared RWX PV+PVC backed by the export so
                                companion workers can reuse it (spec §8.2). When false (default) the
                                operator adds an inline NFSVolumeSource to the pod. MountOptions are only
                                honored in provisioned mode, so setting them forces provisioning.
                              type: boolean
                            readOnly:
                              description: ReadOnly mounts the export read-only (default
                                true for inline NFS).
                              type: boolean
                            server:
                              description: Server is the NFS server hostname or IP.
                              minLength: 1
                              type: string
                            storageClassName:
                              description: StorageClassName for the provisioned PV+PVC.
                              type: string
                          required:
                          - path
                          - server
                          type: object
                        pvc:
                          description: PVC provisions a dedicated PersistentVolumeClaim
                            for this folder.
                          properties:
                            accessModes:
                              description: |-
                                AccessModes for a provisioned PVC. Defaults to ReadWriteOnce for config and
                                ReadWriteMany for shared media.
                              items:
                                type: string
                              type: array
                            existingClaim:
                              description: |-
                                ExistingClaim references an already-provisioned PVC by name. When set, Size
                                and StorageClassName are ignored and the operator creates nothing.
                              type: string
                            mountPath:
                              description: MountPath overrides the default mount path
                                inside the pod.
                              type: string
                            size:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Size is the requested capacity when the
                                operator provisions the PVC.
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            storageClassName:
                              description: StorageClassName selects the storage class
                                for a provisioned PVC.
                              type: string
                          type: object
                        readOnly:
                          description: |-
                            ReadOnly mounts the folder read-only. Defaults vary by source; NFS defaults
                            to read-only.
                          type: boolean
                      required:
                      - mountPath
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of nfs, pvc, or existingClaim must be
                          set
                        rule: '[has(self.nfs), has(self.pvc), has(self.existingClaim)].filter(x,
                          x).size() == 1'
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                type: object
              web:
                description: Web deploys a separate web-tier (jellyfin-web/nginx)
                  Deployment and Service.
                properties:
                  image:
                    description: |-
                      Image is the web-tier container image. When empty the operator does not
                      default; the CR author is expected to set this to the distro web image.
                    type: string
                  podAnnotations:
                    additionalProperties:
                      type: string
                    description: PodAnnotations are merged onto the web pod template
                      metadata.
                    type: object
                  replicas:
                    description: Replicas is the desired number of web-tier pods.
                      Defaults to 1.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources sets the web container resource requests/limits.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  service:
                    description: Service configures the ClusterIP Service fronting
                      the web Deployment.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are applied to the Service (e.g.
                          load-balancer hints).
                        type: object
                      port:
                        description: Port is the Service port exposing Jellyfin. Defaults
                          to 8096.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      type:
                        description: Type is the Service type. Defaults to ClusterIP.
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        type: string
                    type: object
                type: object
            type: object
            x-kubernetes-validations:
            - message: ingress and gateway are mutually exclusive
              rule: '!(has(self.ingress) && has(self.gateway))'
          status:
            description: status defines the observed state of Jellyfin
            properties:
              apiCredentialsSecret:
                description: |-
                  APICredentialsSecret is the name of the operator-owned Secret holding the
                  admin API key minted during bootstrap. The key itself is never surfaced.
                type: string
              conditions:
                description: |-
                  Conditions represent the current state of the Jellyfin resource
                  (Ready, PluginsLoaded, APIReady, LibrariesReady).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                description: Endpoints reports the in-cluster and external addresses
                  of the instance.
                properties:
                  ingress:
                    description: Ingress is the external URL when an Ingress is configured.
                    type: string
                  service:
                    description: Service is the in-cluster URL (http://<svc>.<ns>.svc:8096)
                      the operator uses.
                    type: string
                type: object
              loadedPlugins:
                description: |-
                  LoadedPlugins lists the plugins currently mounted into the running pod
                  template.
                items:
                  description: LoadedPlugin records a plugin mounted into the instance
                    pod template.
                  properties:
                    guid:
                      description: GUID is the plugin GUID.
                      type: string
                    name:
                      description: Name is the JellyfinPlugin resource name.
                      type: string
                    pluginName:
                      description: PluginName is the display name from meta.json /
                        spec.meta.name.
                      type: string
                    version:
                      description: Version is the plugin version currently staged.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              managedLibraries:
                description: |-
                  ManagedLibraries is the set of Jellyfin virtual-folder names the operator
                  currently manages. Prune only ever removes names from this set.
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is a coarse, human-oriented lifecycle summary.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
